# 0.2.0 — computational engine

The foundation is now an executable tensor/flow/proof pipeline. The major
scientific correction is that linear closure ranks are not orbit dimensions.
See [the verification report](verification/REPORT.md) and
[the degree-eight result](docs/DEGREE8_RESULT.md).

Source and actual verification artifacts are committed. The full degree-twelve
research program is not declared finished. The supplied Git bundle contains
local history; applying it is a fast-forward-only operation and does not push.
Use `scripts/apply_engine_bundle.sh` only on a clean intended repository.
